# Install PyTorch separately based on your hardware/OS:
# https://pytorch.org/get-started/locally/
# Example (CPU): pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Core scientific stack
numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
matplotlib>=3.7,<4
Pillow>=10,<12
tqdm>=4.65,<5

# Data
wfdb>=4.1,<5

# LLM + app
openai>=1.0,<2
streamlit>=1.35,<2
